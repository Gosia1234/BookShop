<%= form_for(@sale) do |f| %>
  <% if @sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% @sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :saleTotal %><br>
    <%= f.text_field :saleTotal %>
  </div>
  <div class="field">
    <%= f.label :qtySold %><br>
    <%= f.number_field :qtySold %>
  </div>
  <div class="field">
    <%= f.label :saleDate %><br>
    <%= f.date_select :saleDate %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
